
import { fullWidthLayout } from 'dummy/helpers/full-width-layout';
import { module, test } from 'qunit';

module('Unit | Helper | full width layout', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = fullWidthLayout([42]);
    assert.ok(result);
  });
});

