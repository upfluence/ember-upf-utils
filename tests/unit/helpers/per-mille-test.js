
import { perMille } from 'dummy/helpers/per-mille';
import { module, test } from 'qunit';

module('Unit | Helper | per mille');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = perMille([42]);
  assert.ok(result);
});

