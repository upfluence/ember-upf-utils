
import { signUrl } from 'dummy/helpers/sign-url';
import { module, test } from 'qunit';

module('Unit | Helper | sign url', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = signUrl([42]);
    assert.ok(result);
  });
});

