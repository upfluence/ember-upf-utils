
import { displayError } from 'dummy/helpers/display-error';
import { module, test } from 'qunit';

module('Unit | Helper | display error');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = displayError([42]);
  assert.ok(result);
});

