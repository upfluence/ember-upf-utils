import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('new-account-form', 'Integration | Component | new account form', {
  integration: true
});

test('it renders', function(assert) {
  assert.expect(0);
});
