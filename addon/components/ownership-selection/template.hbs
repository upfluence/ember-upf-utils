{{#if display}}
  {{#power-select options=ownerships selected=ownership onchange=(action (mut ownership))
                 renderInPlace=true searchEnabled=false placeholder="Share with" as |o|}}
    {{o.name}}
  {{/power-select}}
{{/if}}
