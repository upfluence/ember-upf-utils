{{#if display}}
  {{#power-select disabled=disabled allowClear=true options=ownerships selected=ownership onchange=(action (mut ownership))
                 renderInPlace=true searchEnabled=false placeholder="Share with" as |o|}}
    {{o.name}}
  {{/power-select}}
{{/if}}
